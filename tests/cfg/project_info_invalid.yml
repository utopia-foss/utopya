# An invalid project info file used for testing project registration
---
project_name: InvalidTestProject

paths:
  base_dir: "."  # will lead to a logger warning
  models_dir: models

metadata:
  description: A TEST project info file to TEST project registration
  license: LGPLv3+
  language: python3
  misc:
    foo:
      bar: baz
  some_invalid_key: This key should not exist and will lead to an error

run_cfg_format: yaml

py_modules:
  some_module: some_path

output_files:
  foo: bar
